id: '@examples/vue-with-alien-signals'
type: 'application'
stack: 'frontend'

project:
  name: 'Vue Alien Signals Example'
  description: 'Vue Alien Signals Example.'

dependsOn:
  - { id: '@libs/alien-signals-vue' }
  - { id: '@packages-codebase/eslint', scope: 'development' }
  - { id: '@packages-codebase/prettier', scope: 'development' }
  - { id: '@packages-config/typescript', scope: 'development' }

tasks:
  ###########$$$$$$
  ## Development ##
  ###########$$$$$$

  dev:
    command: 'vite dev'
    preset: 'server'

  ###########
  ## Check ##
  ###########

  check:
    deps:
      - '@libs/alien-signals-vue:build'
  check-prettier:
    deps:
      - '@libs/alien-signals-vue:build'
  check-eslint:
    deps:
      - '@libs/alien-signals-vue:build'
  check-types:
    deps:
      - '@libs/alien-signals-vue:build'

  ############
  ## Format ##
  ############

  format:
    deps:
      - '@libs/alien-signals-vue:build'
  format-prettier:
    deps:
      - '@libs/alien-signals-vue:build'
  format-eslint:
    deps:
      - '@libs/alien-signals-vue:build'
