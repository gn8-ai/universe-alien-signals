id: '@packages-codebase/eslint'
type: 'library'
stack: 'unknown'

project:
  description: 'ESLint configuration for codebase.'

dependsOn:
  - { id: '@packages-config/typescript' }
  - { id: '@packages-codebase/prettier', scope: 'development' }
