id: '@packages-codebase/prettier'
type: 'library'
stack: 'unknown'

project:
  description: 'Prettier configuration for codebase.'

dependsOn:
  - { id: '@packages-config/typescript' }
  - { id: '@packages-codebase/eslint', scope: 'development' }
